<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing leaves the device. The DB is SQLCipher-encrypted under a
  non-extractable Keystore key (TDD §6.1), so a cloud copy would be
  unrestorable anyway — and the ledger is not something to hand to a
  backup transport. Backup is the logical export in §9 instead.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
