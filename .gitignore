# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties

# Android / SDK
local.properties
*.apk
*.aab
*.ap_
*.dex
release/
captures/

# Signing — never commit these
*.jks
*.keystore
keystore.properties

# IDE
.idea/
*.iml
.kotlin/

# OS
.DS_Store
Thumbs.db

# .md
handoff.md

# Raw notification dumps pulled off the device. These hold real amounts, real
# merchant names and the account holder's own name — the committed fixtures in
# src/test/resources/notifications/ are the redacted versions of the same data.
corpus-raw/